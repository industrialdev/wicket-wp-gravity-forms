// -----------
// Editor code
// -----------

// This script handles interactions with Wicket custom field settings in the Gravity Forms editor.
// The field settings themselves are automatically registered via PHP get_form_editor_field_settings() methods,
// so no manual JavaScript registration is needed.

// Initialize on document ready
jQuery(document).ready(function () {
    // Any custom editor interactions can be added here
    console.log("Wicket GF Editor script loaded");
});
